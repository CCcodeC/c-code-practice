#include<stdio.h>
int f(int n)
{
    if(n==10)
    return 1;
    else
    return ((f(n+1)+1)*2);
}
int main()
{
    for(int i=10;i>=1;i--)
    {
        int x= f(i);
        printf("%d\n",x);
    }
    return 0;
}
