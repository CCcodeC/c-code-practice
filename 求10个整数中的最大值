#include<stdio.h>
int main()
{
    int arr[]={1,213,5,6,5,8,4,6,1};
    int max=arr[0];
    int sz=sizeof(arr)/sizeof(arr[0]);
    for(int i=0;i<=sz;i++)
    {
        if(arr[i]>max)
        {
            max=arr[i];
        }
    }
    printf("%d\n",max);    
    return 0;
}
