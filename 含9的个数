#include<stdio.h>
int main()
{
	int x = 0;
	int count = 0;
	for (x = 0; x <= 100; x++)
	{
		if (x % 10 == 9||x/10==9)
		{
			printf("%d\n", x);
			count++;
		}
	}
	printf("\ncount=%d\n", count);
	return 0;
}
